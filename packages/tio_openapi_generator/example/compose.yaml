version: 3.8.0

services:
  swagger:
    image: swaggerapi/swagger-ui
    restart: always
    ports:
      - 80:8080
    volumes:
      - ./openapi.yaml:/openapi.yaml
    environment:
      SWAGGER_JSON: /openapi.yaml
